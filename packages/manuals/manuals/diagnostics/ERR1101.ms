# ERR1101 — Unsupported JSX element

## Problem

A JSX name or runtime element type is neither a supported lowercase Markdown
intrinsic nor an uppercase component.

## Fix

Use Markdown syntax, a supported intrinsic, or an imported or locally declared
uppercase component. Use directives for metadata and language-labelled fenced
code for consumer-owned source.

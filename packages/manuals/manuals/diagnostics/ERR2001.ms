# ERR2001 — Bun bundler is unavailable

## Problem

Bundled compilation requires a runtime that exposes Bun's build API.

## Fix

Run the command with Bun, or use non-bundled compilation when the Bun build API is unavailable.
